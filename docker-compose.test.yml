# Docker Compose for dafke install test
# Runs a clean-room install + smoke test on minimal Linux.
#
# Usage: docker compose -f docker-compose.test.yml up --build
#    or: npm run test:docker

services:
  install-test:
    build: .
    environment:
      - NODE_ENV=test
      - CI=true
